class Product {
  constructor(id, ownerId, title, imgUrl, desc, price) {
    this.id = id;
    this.ownerId = ownerId;
    this.title = title;
    this.imgUrl = imgUrl;
    this.desc = desc;
    this.price = price;
  }
}

export default Product;
